import styled from 'styled-components'

export const CardContainer = styled.div`
	width: 100%;
	height: auto;
	padding: 1.5rem;
	margin: 0;

	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: flex-start;

	background-color: white;
	box-shadow: 0 0.5rem 1.25rem 0rem rgba(0, 0, 0, 0.15);
	border-radius: 1rem;
`
